# graph-fraud-detection
A Graph Neural Network (GCN + GATv2) approach to financial fraud detection,modeling the IEEE-CIS transaction dataset as a heterogeneous graph to catchfraud rings that tabular models miss.
